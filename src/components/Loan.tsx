import { IformData } from "../routes/Register";

interface IloanProps{
    children: IformData
}
const Loan: React.FC<IloanProps> = ({children}) => {
    return(
        <div>
            
        </div>
    );
}